---
description: 本仓库的迭代、分支与成稿交付约定
alwaysApply: true
---

# 仓库约定

- 只在 `main` 上迭代。不要新建功能分支，不要为后续改稿开 PR。
- 每轮改稿后：更新 Markdown 正文 → 运行 `./生成Word.sh` 或 `python3 scripts/build_formatted_docx.py` → 提交并 `git push origin main`。
- 可下载文件：`paper.docx`（当前最新）。同时保留轮次副本 `paper_vN.docx`（如 `paper_v3.docx`），避免覆盖后无法对照。
- 正文来源：`失衡·校准·均衡_AI赋能小学语文三元协同师生互动优化策略.md`。Word 由 `scripts/build_formatted_docx.py` 按 `scripts/word_format.py` 生成。
- 文中不出现作者姓名、单位、基金、联系方式。个人资料与执教者信息不引用、不标注。
- 不要把无样本、无操作性定义的数字写成统计结论。公开报道数据必须标明「非本文实验数据」。
- 改稿、润色或去 AI 化时，先读并遵循 `.cursor/skills/teaching-paper-zh/SKILL.md` 与 `.cursor/skills/de-ai-academic-zh/SKILL.md`。
